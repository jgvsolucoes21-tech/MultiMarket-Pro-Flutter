name: Flutter Build APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.13.0'
          channel: 'stable'
      
      - name: Create project structure
        run: |
          flutter create --org com.jgvsolutions --project-name multimarket_pro flutter_app
          cd flutter_app
          
          # Copy your dart files
          cp ../main.dart lib/
          cp ../login_page.dart lib/
          cp ../account_page.dart lib/
          cp ../splash_page.dart lib/
          cp ../avatar.dart lib/
          cp ../constants.dart lib/
          cp ../pubspec.yaml .
          
          cd ..
          cp -r flutter_app/* .
      
      - name: Get dependencies
        run: flutter pub get
      
      - name: Build APK
        run: flutter build apk --release
      
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release.apk
          path: build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30
