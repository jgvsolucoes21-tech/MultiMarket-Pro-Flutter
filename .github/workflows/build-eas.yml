name: Build & Submit Android (EAS)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: react-native-app

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          # cache desactivado → más simple, más estable, cero dramas

      - name: Instalar Expo y EAS CLI
        run: npm install -g expo-cli eas-cli

      - name: Instalar dependencias (modo definitivo)
        run: |
          rm -f package-lock.json
          npm install

      - name: Login Expo / EAS
        run: eas login --token ${{ secrets.EXPO_TOKEN }}

      - name: Build Android AAB
        run: eas build --platform android --profile production --non-interactive --no-wait

      - name: ¡GANAMOS!
        run: echo "Build lanzado. Andá a festejar, campeón."
